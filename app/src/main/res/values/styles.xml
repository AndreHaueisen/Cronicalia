<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/colorPrimaryLight</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="EditTextDark" parent="@android:style/Widget.Material.EditText">
        <!--<item name="android:fontFamily">@font/lato</item>-->
        <item name="android:dropDownSelector">@color/colorAccent</item>
        <item name="android:textColorHint">@color/colorPrimaryLight</item>
        <item name="android:textColor">@color/white_android</item>
        <item name="android:completionThreshold">2</item>
        <!--<item name="android:textCursorDrawable">@drawable/shape_cursor</item>-->
    </style>

    <style name="RadioButtonWhite" parent="Widget.AppCompat.CompoundButton.RadioButton">
        <item name="android:colorControlNormal">@color/white_android</item>
        <item name="android:colorControlHighlight">@color/white_android</item>
        <item name="android:textSize">14sp</item>
    </style>

</resources>
